# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on Staging
on:
  push:
    branches:
      - staging
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PRUEBA_STAGING_IDP_13GO }}
          channelId: live
          projectId: prueba-staging-idp-13go
      - name: Reemplaza Parametros Staging
        run:  | 
          sed -i -e 's/apiKey: "0987654321"/apiKey: "1234567890"/g' public/index.html
          sed -i -e 's/authDomain: "staging.13.cl"/authDomain: "staging.13.cl"/g' public/index.html